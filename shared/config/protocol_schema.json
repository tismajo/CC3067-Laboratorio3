{
  "_comment": "Editar SOLO este archivo si el dia de la prueba hay que renombrar campos, agregar un nuevo 'type'/'proto', o cambiar el TTL por defecto. Nadie debe hardcodear estos strings en el codigo: siempre usar shared/constants.py.",

  "field_names": {
    "version": "version",
    "proto": "proto",
    "type": "type",
    "from": "from",
    "to": "to",
    "ttl": "ttl",
    "headers": "headers",
    "payload": "payload"
  },

  "proto_values": {
    "DIJKSTRA": "dijkstra",
    "FLOODING": "flooding",
    "LSR": "lsr"
  },

  "type_values": {
    "HELLO": "hello",
    "ECHO": "echo",
    "MESSAGE": "message",
    "INFO": "info"
  },

  "defaults": {
    "ttl": 16,
    "hello_ttl": 1
  },

  "required_fields": ["version", "proto", "type", "from", "to", "ttl", "payload"]
}
