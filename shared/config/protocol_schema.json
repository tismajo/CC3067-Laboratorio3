{
  "_comment": "Formato en el cable del Protocolo Unificado (ver PROTOCOLO.md). Editar SOLO este archivo si cambia el formato acordado. Nadie debe hardcodear estos strings: usar shared/constants.py o shared/protocol.py.",

  "version": 1,

  "field_names": {
    "version": "version",
    "proto": "proto",
    "type": "type",
    "from": "from",
    "to": "to",
    "ttl": "ttl",
    "headers": "headers",
    "payload": "payload"
  },

  "proto_values": {
    "DIJKSTRA": "dijkstra",
    "FLOODING": "flooding",
    "LSR": "lsr"
  },

  "type_values": {
    "HELLO": "hello",
    "ECHO": "echo",
    "MESSAGE": "message",
    "INFO": "info"
  },

  "header_keys": {
    "MSG_ID": "msg_id",
    "CHECKSUM": "checksum",
    "T0": "t0",
    "VIA": "via",
    "TRACE": "trace"
  },

  "defaults": {
    "ttl": 16,
    "hello_ttl": 1
  },

  "limits": {
    "max_line_bytes": 65536
  },

  "broadcast_to": "*",

  "required_fields": ["proto", "type", "from", "to", "ttl", "headers", "payload"],
  "required_headers": ["msg_id", "checksum"]
}
